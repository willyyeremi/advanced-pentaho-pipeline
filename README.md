# Advanced Pentaho Pipeline

This project will use systematic pipeline with Pentaho combined with dbt for doing unit test to the data

## Requirement

You need:

- pipx to install executeable packages. The list of packages are:
  - dbt-core
  - dbt-postgres
- dbt-core and dbt-postgres for data unit test
- postgresql as database to store logs and data
